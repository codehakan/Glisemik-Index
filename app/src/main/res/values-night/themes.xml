<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GlisemikIndeks" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/index_high</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item> <!-- Status bar arka plan rengi -->
        <item name="android:windowLightStatusBar">true</item>   <!--  Status bar beyaz olduğu için yazı rengini otomatik değiştiriyor.-->
        <!-- Customize your theme here. -->
        <!-- <item name="android:editTextColor">@android:color/white</item>-->
        <!-- <item name="android:textColorHint">@color/white</item>-->
        <item name="android:forceDarkAllowed" tools:targetApi="Q">false</item>
    </style>
</resources>